{% extends 'base.html.twig' %}

{% block title %} Créer catégorie {% endblock %}

{% block body %}

    <section class="container">
        <h2 class="text-primary h2">Création d'une nouvelle catégorie</h2>
        {{ form_start(form_view) }}
        {{ form_widget(form_view) }}
        {{ form_end(form_view) }}
    </section>
    <section class="container mt-3">
        <a href="{{ path('category') }}" class="btn btn-primary">Retourner à la vue des catégories</a>
    </section>

{% endblock %}