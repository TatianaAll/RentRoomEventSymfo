{% extends 'base.html.twig' %}

{% block title %} Modification {{ event.title}} {% endblock %}

{% block body %}

    <section class="container">
        <h2 class="text-primary h2">Modification de l'évènement '{{ event.title }}'</h2>
        {{ form_start(form_view) }}
        {{ form_widget(form_view) }}
        {{ form_end(form_view) }}
    </section>
    <section class="container mt-3">
        <a href="{{ path('room') }}" class="btn btn-primary">Retourner aux évènements</a>
    </section>

{% endblock %}